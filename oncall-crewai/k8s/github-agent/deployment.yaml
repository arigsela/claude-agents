# Deployment for GitHub/GitOps A2A Agent
# NOTE: No ClusterRole/RBAC — this agent has no Kubernetes API access
apiVersion: apps/v1
kind: Deployment
metadata:
  name: github-agent-a2a
  namespace: oncall-crewai
  labels:
    app: github-agent-a2a
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: github-agent-a2a
  template:
    metadata:
      labels:
        app: github-agent-a2a
        version: v1
    spec:
      serviceAccountName: github-gitops-agent

      containers:
      - name: agent
        image: 852893458518.dkr.ecr.us-east-2.amazonaws.com/crewai-github-agent:latest
        imagePullPolicy: Always

        ports:
        - name: http
          containerPort: 8080
          protocol: TCP

        envFrom:
        - configMapRef:
            name: github-agent-config

        env:
        - name: ANTHROPIC_API_KEY
          valueFrom:
            secretKeyRef:
              name: github-agent-secrets
              key: anthropic-api-key

        - name: GITHUB_TOKEN
          valueFrom:
            secretKeyRef:
              name: github-agent-secrets
              key: github-token

        - name: API_KEYS
          valueFrom:
            secretKeyRef:
              name: github-agent-secrets
              key: api-keys

        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"

        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 60
          timeoutSeconds: 10
          failureThreshold: 5

        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 15
          timeoutSeconds: 10
          failureThreshold: 5

      terminationGracePeriodSeconds: 30

---
# ServiceAccount for GitHub agent (no ClusterRole — no K8s access)
apiVersion: v1
kind: ServiceAccount
metadata:
  name: github-gitops-agent
  namespace: oncall-crewai
  labels:
    app: github-agent-a2a
    component: rbac

---
# Service for GitHub/GitOps Agent
apiVersion: v1
kind: Service
metadata:
  name: github-agent-a2a
  namespace: oncall-crewai
  labels:
    app: github-agent-a2a
spec:
  type: ClusterIP
  selector:
    app: github-agent-a2a
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
