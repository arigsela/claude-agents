# ==============================================================================
# Python Project Configuration (pyproject.toml)
# ==============================================================================
# This is the modern standard for Python project metadata (PEP 621).
# It replaces the older setup.py / setup.cfg approach.
# ==============================================================================

[project]
name = "chores-tracker-agent"
version = "1.0.0"
description = "AI agent with deep knowledge of the Chores Tracker application for answering questions about architecture, API, deployment, and troubleshooting"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py311"
