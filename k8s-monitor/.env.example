# Claude API Configuration
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# LLM Model Configuration
# Choose models based on task complexity and cost optimization
#
# Haiku 4.5: Fast, cost-effective, good for simple tasks
# Sonnet 4.5: Balanced performance, better reasoning, more expensive
#
# ⚠️ IMPORTANT: ALL MODELS HARDCODED TO HAIKU FOR COST OPTIMIZATION
# The Python code in src/orchestrator/monitor.py uses hardcoded Haiku constants
# These environment variables are used by the Agent SDK when loading .claude/agents/*.md files
# ALL must be Haiku to ensure no Sonnet usage (12x cost reduction)

ORCHESTRATOR_MODEL=claude-haiku-4-5-20251001
K8S_ANALYZER_MODEL=claude-haiku-4-5-20251001
ESCALATION_MANAGER_MODEL=claude-haiku-4-5-20251001
SLACK_NOTIFIER_MODEL=claude-haiku-4-5-20251001
GITHUB_REVIEWER_MODEL=claude-haiku-4-5-20251001

# Kubernetes Configuration
KUBECONFIG=/path/to/your/k3s/kubeconfig
K3S_CONTEXT=default

# GitHub Configuration (for deployment correlation)
GITHUB_TOKEN=your_github_personal_access_token
GITHUB_REPO_OWNER=arigsela
GITHUB_REPO_NAME=kubernetes

# Slack Configuration (Optional - set SLACK_ENABLED=false to disable)
SLACK_ENABLED=true
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
SLACK_CHANNEL=#infrastructure-alerts

# Monitoring Configuration
# Interval in minutes (default: 60 = 1 hour)
# Examples: 5 = every 5 minutes, 15 = every 15 minutes, 60 = every hour
MONITORING_INTERVAL_MINUTES=60

# MCP Server Paths (optional, defaults to docs/mcp-reference/)
# GITHUB_MCP_PATH=/custom/path/to/github-mcp-server/dist/index.js
# SLACK_MCP_PATH=/custom/path/to/slack-mcp-server/dist/index.js

# Cost Optimization Notes:
#
# Haiku vs Sonnet pricing (approximate):
# - Haiku: ~$0.25 per million input tokens
# - Sonnet: ~$3.00 per million input tokens (12x more expensive)
#
# Example cost per monitoring cycle (ACTUAL - All Haiku):
# - All Haiku: ~$0.001-0.003 per cycle (CURRENT CONFIGURATION)
# - All Sonnet: ~$0.12 per cycle (NOT USED - for reference)
#
# With hourly monitoring (24 cycles/day, 720 cycles/month):
# - All Haiku: ~$0.72-2.16/month (~$9-26/year) ✅ CURRENT
# - All Sonnet: ~$86.40/month (~$1,036/year) - NOT USED
#
# Annual Savings: ~$1,000 by using all Haiku!
